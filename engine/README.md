# /engine — saf hesaplama

Bu klasördeki hiçbir dosya veritabanı, `fetch`, dosya sistemi veya React içe aktarmaz.
Girdi alır, çıktı verir.

Sebebi: test edilebilirlik, mobil uygulamada yeniden kullanılabilirlik ve motor
sürümleri arasında karşılaştırma yapabilmek.

Buraya gelecek olanlar: `compatibility.ts` (SCHEMA.md bölüm 7),
`performance.ts` (SCHEMA.md bölüm 8).
